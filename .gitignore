# Node (frontend tooling run from the repo root, e.g. `pnpm dlx`)
node_modules/

# Rust (Cargo workspace: crates/*)
/target/
# tauri-build generated capability schemas (written on desktop-OS builds only)
/crates/desktop/gen/schemas/

# Frontend (Tauri SPA — standalone pnpm project)
/frontend/dist/

# Editor / OS
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.DS_Store
Thumbs.db

# Local scratch
/tmp/
*.log
