# Deno
.deno/
deno.lock.bak

# npm — workspace-root node_modules (packages/ui Vite member; nodeModulesDir=auto)
node_modules/

# Build / dist
/dist/
/build/
/desktop/        # `deno desktop` default bundle output (regenerate with `deno task desktop`)
*.exe
*.dll
denort*

# Rust (Cargo workspace: crates/*)
/target/

# Frontend (Tauri SPA — pnpm project independent of the Deno tree)
/frontend/dist/

# Editor / OS
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.DS_Store
Thumbs.db

# Local toolchain — downloaded Deno >= 2.9 (devbox global caps at 2.8.3; `deno desktop` needs 2.9)
/bin/deno
/bin/deno.exe

# Local scratch
/tmp/
*.log
